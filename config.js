/* =========================================================
   KRANGLE CAPITAL — CONFIG
   Replace the two values below with YOUR values from Supabase
   (Settings -> API). Both are safe to be public.
   Keep the quotes. Don't change anything else.
   ========================================================= */
window.KC_CONFIG = {
  SUPABASE_URL:      "PASTE_YOUR_PROJECT_URL_HERE",   // e.g. https://abcd1234.supabase.co
  SUPABASE_ANON_KEY: "PASTE_YOUR_ANON_PUBLIC_KEY_HERE" // the long "anon / public" key
};
